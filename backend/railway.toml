[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=/app:$PYTHONPATH alembic upgrade head && PYTHONPATH=/app:$PYTHONPATH uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
